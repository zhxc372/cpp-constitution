-- {{ project_name }}
-- Generated by cpp-constitution

set_languages("c++{{ std_version }}")
{% if not exceptions %}
add_cxxflags("-fno-exceptions")
{% endif %}

target("{{ project_name }}")
    set_kind("binary")
    add_files("src/*.cpp")
    add_headerfiles("include/(**.hpp)", "include/(**.h)")
    add_includedirs("include")
