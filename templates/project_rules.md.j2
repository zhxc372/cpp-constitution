# Project Rules — {{ project_name }}

> Auto-generated by cpp-constitution. Edit to customize.

---

## Build & Verify

{% if build == "cmake" %}
```bash
cmake -B build -S . -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build
ctest --test-dir build --output-on-failure
```
{% elif build == "make" %}
```bash
bear -- make
make
make test
```
{% elif build == "xmake" %}
```bash
xmake f -m release
xmake project -k compile_commands
xmake build
xmake run
```
{% elif build == "meson" %}
```bash
meson setup build
meson compile -C build
meson test -C build
```
{% elif build == "autotools" %}
```bash
bear -- ./autogen.sh && ./configure && make
make check
```
{% else %}
```bash
# No build system configured. Define your own build commands.
```
{% endif %}

## Static Analysis

```bash
# If clang-tidy is available
clang-tidy -p build src/*.cpp
```

## C++ Constraints

- **Standard:** {{ std }}
{% if not exceptions %}
- **Exceptions:** DISABLED (`{{ exceptions_flag }}`)
- Forbidden: `throw`, `try`, `catch`, `std::exception`, `std::runtime_error`
- Use: error codes, `std::expected` (C++23), `std::optional`, `std::variant`
{% endif %}

## File Layout

```text
{{ project_name }}/
├── CONSTITUTION.md              # This constitution
├── project_rules.compiled.md    # Compiled constraints
├── AGENTS.md                    # Universal AI rules entry
├── references/                  # Detailed C++ rules
├── config/                      # clang-tidy profiles
├── scripts/validate.sh          # Project validation
├── GOTCHAS.md                   # Known AI failure patterns
├── src/                         # Source code
├── include/                     # Headers
└── tests/                       # Tests
```
